# URL publique du serveur Socket.io déployé sur Railway.
# À définir dans Vercel : Settings → Environment Variables → RIDENOW_BACKEND
RIDENOW_BACKEND=https://ridenow-production.up.railway.app

# (Serveur / Railway) Le port est fourni automatiquement par Railway via PORT.
# PORT=3000
