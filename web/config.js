/**
 * URL du backend RideNow.
 * - En PROD (Vercel) : ce fichier est REGÉNÉRÉ au build depuis la variable
 *   d'environnement RIDENOW_BACKEND (voir build.js). Pas besoin d'y toucher.
 * - En LOCAL : laisse vide, le serveur Node sert le front en même origine.
 */
window.RIDENOW_BACKEND = "";
